<Window x:Class="JNR.Views.Profile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JNR.Views"
        xmlns:models="clr-namespace:JNR.Models"
        xmlns:converters="clr-namespace:JNR.Converters"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="User Profile - JNR"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <FontFamily x:Key="DefaultFont">Segoe UI</FontFamily>
        <FontFamily x:Key="TitleFont">/Fonts/#Dosis</FontFamily>
        <SolidColorBrush x:Key="LightTextColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="MediumTextColor" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="DarkTextColor" Color="#808080"/>
        <SolidColorBrush x:Key="AccentColorBlue" Color="#462AD8"/>
        <!-- Using global converters from App.xaml -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="Button" x:Key="WindowControlButtonBase" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="25"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="MinimizeButtonStyle" BasedOn="{StaticResource WindowControlButtonBase}">
            <Setter Property="Content" Value=""/>
            <Setter Property="Background" Value="#FF4E72B8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF6889CA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="CloseButtonStyle" BasedOn="{StaticResource WindowControlButtonBase}">
            <Setter Property="Content" Value=""/>
            <Setter Property="Background" Value="IndianRed"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightCoral"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ListBoxItem" x:Key="ProfileAlbumListBoxItemStyle" BasedOn="{StaticResource NoHighlightListBoxItem}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#19FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Viewbox Stretch="Uniform">
        <Border CornerRadius="10" Background="Transparent">
            <Border CornerRadius="10" BorderThickness="1">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#462AD8" Offset="0"/>
                        <GradientStop Color="#DA34A3" Offset="0.75"/>
                        <GradientStop Color="#8A16C1" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                        <GradientStop Color="#100E1E" Offset="0"/>
                        <GradientStop Color="#141228" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid Width="800" Height="450">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Column="0" Grid.Row="0">
                        <Button Name="btnGoBack" Click="btnGoBack_Click" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Padding="0" Cursor="Hand">
                            <Image Source="/Images/rewind.png" Stretch="Uniform" Width="18" Height="18" Opacity="0.8"/>
                        </Button>
                        <TextBlock Text="Profile" VerticalAlignment="Center" FontWeight="Light" HorizontalAlignment="Right" Foreground="{StaticResource LightTextColor}" FontSize="20" FontFamily="{StaticResource TitleFont}" Margin="0,0,25,0"/>
                    </Grid>

                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Style="{StaticResource MinimizeButtonStyle}" Click="MinimizeButton_Click" Margin="0,0,5,0"/>
                        <Button Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
                    </StackPanel>

                    <StackPanel x:Name="SidebarContentPanel" Grid.Column="0" Grid.Row="1">
                        <RadioButton Content="My Albums" Height="50" Tag="/Images/SideBarIcons/folder-user.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="MyAlbums"/>
                        <RadioButton Content="Genres" Height="50" Tag="/Images/SideBarIcons/radio.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="Genres"/>
                        <RadioButton Content="Charts" Height="50" Tag="/Images/SideBarIcons/rows-plus-top.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="Charts"/>
                        <RadioButton Content="About" Height="50" Tag="/Images/SideBarIcons/question.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="About"/>
                        <RadioButton Content="Settings" Height="50" Tag="/Images/SideBarIcons/gear.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="Settings"/>
                    </StackPanel>
                    <!-- Icons for Sidebar -->
                    <Image Source="/Images/SideBarIcons/folder-user.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,14,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/radio.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,64,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/rows-plus-top.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,114,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/question.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,164,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/gear.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,214,0,0" IsHitTestVisible="False"/>

                    <!-- Main Content Area -->
                    <Grid Grid.Column="1" Grid.Row="1" Margin="15,10,15,10">
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MediumTextColor}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding ShowStatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}" TextWrapping="Wrap"/>

                        <Grid Visibility="{Binding ShowContent, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Profile Header -->
                            <StackPanel Grid.Row="0" Margin="5,0,0,15">
                                <TextBlock Text="{Binding ProfileUsername}" FontSize="28" FontWeight="Bold" FontFamily="{StaticResource TitleFont}" Foreground="White"/>
                                <TextBlock Text="{Binding MemberSince}" FontSize="12" Foreground="{StaticResource MediumTextColor}"/>
                            </StackPanel>

                            <!-- Stats Bar -->
                            <Border Grid.Row="1" Background="#1A2035" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                        <TextBlock Text="ALBUMS IN COLLECTION" FontSize="10" Foreground="{StaticResource DarkTextColor}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding TotalCollectionCount}" FontSize="20" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                        <TextBlock Text="AVERAGE RATING GIVEN" FontSize="10" Foreground="{StaticResource DarkTextColor}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding AverageRatingGiven}" FontSize="20" Foreground="#FFD700" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Tabbed Content -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Album Collection List -->
                                <ListBox Grid.Column="0" ItemsSource="{Binding UserAlbums}" Background="Transparent" BorderThickness="0"
                                         ItemContainerStyle="{StaticResource ProfileAlbumListBoxItemStyle}"
                                         ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemTemplate>
                                        <!-- CORRECTED DataType -->
                                        <DataTemplate DataType="{x:Type models:ProfileAlbumItem}">
                                            <Border Background="#1A2035" CornerRadius="5" Padding="10" Margin="0,0,10,8" Cursor="Hand" MouseLeftButtonUp="Album_Click">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Image Grid.Column="0" Source="{Binding CoverArtUrl, FallbackValue='/Images/placeholder_album.png'}" Width="60" Height="60" Stretch="Uniform" VerticalAlignment="Center"/>
                                                    <StackPanel Grid.Column="1" Margin="12,0,10,0" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding AlbumName}" FontWeight="Bold" Foreground="{StaticResource LightTextColor}" FontSize="14" MaxWidth="220" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding ArtistName}" Foreground="{StaticResource MediumTextColor}" FontSize="11" MaxWidth="220" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="{Binding UserRatingDisplay}" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#FFD700" FontSize="16" FontWeight="Bold"
                                                               Visibility="{Binding HasUserRating, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>

                                <!-- Artist Stats Chart -->
                                <StackPanel Grid.Column="1" Margin="10,0,0,0" Visibility="{Binding ShowChart, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Most Collected Artists" FontSize="16" FontFamily="{StaticResource TitleFont}" Foreground="{StaticResource LightTextColor}" Margin="5,0,0,10"/>
                                    <lvc:CartesianChart Series="{Binding UserAlbumsChartSeries}" LegendLocation="None" Height="250" Hoverable="False">
                                        <lvc:CartesianChart.DataTooltip>
                                            <lvc:DefaultTooltip BulletSize="10" Background="#2A3045" Foreground="{StaticResource LightTextColor}"/>
                                        </lvc:CartesianChart.DataTooltip>
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis Labels="{Binding UserAlbumsChartLabels}" Foreground="{StaticResource MediumTextColor}" FontSize="9">
                                                <lvc:Axis.Separator>
                                                    <lvc:Separator StrokeThickness="0" />
                                                </lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis Title="Count" MinValue="0" Foreground="{StaticResource MediumTextColor}" ShowLabels="True">
                                                <lvc:Axis.Separator>
                                                    <lvc:Separator Stroke="{StaticResource DarkTextColor}" StrokeThickness="0.5" Step="1" />
                                                </lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Border>
    </Viewbox>
</Window>