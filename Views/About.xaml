<!-- File: Views/About.xaml -->
<Window x:Class="JNR.Views.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JNR.Views"
        mc:Ignorable="d"
        Title="About JamNRate - JNR"
        WindowStyle="None"
        ResizeMode="CanResize" 
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Using global styles/converters from App.xaml if available -->
        <!-- Local fallbacks or specific styles for this window -->
        <FontFamily x:Key="DefaultFont">Segoe UI</FontFamily>
        <FontFamily x:Key="TitleFont">/Fonts/#Dosis</FontFamily>
        <SolidColorBrush x:Key="LightTextColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="MediumTextColor" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="DarkTextColor" Color="#808080"/>
        <SolidColorBrush x:Key="AccentColorLink" Color="#6BB5FF"/>

        <Style TargetType="Button" x:Key="WindowControlButtonBase" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="25"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="MinimizeButtonStyle" BasedOn="{StaticResource WindowControlButtonBase}">
            <Setter Property="Content" Value=""/>
            <!-- Minimize icon -->
            <Setter Property="Background" Value="#FF4E72B8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF6889CA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="CloseButtonStyle" BasedOn="{StaticResource WindowControlButtonBase}">
            <Setter Property="Content" Value=""/>
            <!-- Close icon -->
            <Setter Property="Background" Value="IndianRed"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightCoral"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Hyperlink">
            <Setter Property="Foreground" Value="{StaticResource AccentColorLink}"/>
            <Setter Property="TextDecorations" Value="None"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="TextDecorations" Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource DarkTextColor}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="0,15,0,15"/>
        </Style>
    </Window.Resources>

    <Viewbox Stretch="Uniform">
        <Border CornerRadius="10" Background="Transparent">
            <Border CornerRadius="10" BorderThickness="1">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#462AD8" Offset="0"/>
                        <GradientStop Color="#DA34A3" Offset="0.75"/>
                        <GradientStop Color="#8A16C1" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                        <GradientStop Color="#100E1E" Offset="0"/>
                        <GradientStop Color="#141228" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid Width="800" Height="450">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Top Bar Content -->
                    <TextBlock Grid.Column="0" Grid.Row="0" Text="About"
                               VerticalAlignment="Center" FontWeight="Light" FontSize="20"
                               HorizontalAlignment="Center" Foreground="{StaticResource LightTextColor}"
                               FontFamily="{StaticResource TitleFont}"/>

                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Style="{StaticResource MinimizeButtonStyle}" Click="MinimizeButton_Click" Margin="0,0,5,0"/>
                        <Button Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
                    </StackPanel>

                    <!-- Sidebar Content -->
                    <StackPanel Grid.Column="0" Grid.Row="1">
                        <RadioButton Content="My Albums" Height="50" Tag="/Images/SideBarIcons/folder-user.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="MyAlbums"/>
                        <RadioButton Content="Genres" Height="50" Tag="/Images/SideBarIcons/radio.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="Genres"/>
                        <RadioButton Content="Charts" Height="50" Tag="/Images/SideBarIcons/rows-plus-top.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="Charts"/>
                        <RadioButton Content="About" IsChecked="True" Height="50" Tag="/Images/SideBarIcons/question.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}"/>
                        <RadioButton Content="Settings" Height="50" Tag="/Images/SideBarIcons/gear.png" Foreground="White" FontSize="12" Style="{StaticResource MainButtonTheme}" Click="SidebarNavigation_Click" CommandParameter="Settings"/>
                        <Button Content="Back to Search" Height="40" Margin="10,20,10,0" Background="#303040" Foreground="White" Click="GoToSearch_Click"/>
                    </StackPanel>
                    <!-- Sidebar Icon Images (visual only, actual icons via Tag) -->
                    <Image Source="/Images/SideBarIcons/folder-user.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,15,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/radio.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,65,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/rows-plus-top.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,115,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/question.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,165,0,0" IsHitTestVisible="False"/>
                    <Image Source="/Images/SideBarIcons/gear.png" Grid.Row="1" Grid.Column="0" Width="20" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,215,0,0" IsHitTestVisible="False"/>


                    <!-- Main Content Area for About -->
                    <ScrollViewer Grid.Column="1" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="5">
                        <StackPanel Margin="20,15,20,20">

                            <Image Source="/Images/logo.png" Width="70" Height="70" HorizontalAlignment="Center" Margin="0,0,0,10" RenderOptions.BitmapScalingMode="Fant"/>
                            <TextBlock Text="JamNRate" FontSize="26" FontWeight="Bold" FontFamily="{StaticResource TitleFont}" HorizontalAlignment="Center" Foreground="{StaticResource LightTextColor}"/>
                            <TextBlock x:Name="VersionTextBlock" Text="Version 1.0.0" FontSize="13" HorizontalAlignment="Center" Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,5"/>
                            <TextBlock Text="Your personal companion for discovering, rating, and tracking music albums." 
                                       FontSize="12" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" 
                                       Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,15" MaxWidth="450"/>

                            <Separator/>

                            <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource LightTextColor}" Margin="0,0,0,3">Developed By</TextBlock>
                            <TextBlock Text="Daniel Acosta Castilla" FontSize="13" Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,15"/>

                            <Separator/>

                            <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource LightTextColor}" Margin="0,0,0,8">Data Sources  APIs</TextBlock>
                            <TextBlock FontSize="13" Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,3" TextWrapping="Wrap">
                                <Run Text="Music data, artist information, and images primarily provided by the "/>
                                <Hyperlink NavigateUri="https://www.last.fm/api" RequestNavigate="Hyperlink_RequestNavigate">Last.fm API</Hyperlink><Run Text="."/>
                            </TextBlock>
                            <TextBlock FontSize="13" Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,3" TextWrapping="Wrap">
                                <Run Text="Detailed album information, tracklists, release years, and discography data sourced from the "/>
                                <Hyperlink NavigateUri="https://www.discogs.com/developers" RequestNavigate="Hyperlink_RequestNavigate">Discogs API</Hyperlink><Run Text="."/>
                            </TextBlock>
                            <TextBlock Text="This application is not affiliated with, endorsed, or sponsored by Last.fm or Discogs. Please refer to their respective terms of service." 
                                       FontSize="10" FontStyle="Italic" Foreground="{StaticResource DarkTextColor}" Margin="0,5,0,15" TextWrapping="Wrap"/>

                            <Separator/>

                            <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource LightTextColor}" Margin="0,0,0,8"></TextBlock>
                            <TextBlock Text="Built with .NET WPF and C# using Visual Studio." FontSize="13" Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,3"/>
                            <TextBlock Text="Fonts: Dosis (Titles), Segoe UI (Body)." FontSize="13" Foreground="{StaticResource MediumTextColor}" Margin="0,0,0,15"/>
                            <!-- Add more specific credits if needed -->

                            <Separator/>

                            <TextBlock x:Name="CopyrightTextBlock" Text="© YYYY [Your Name/Alias Here]. All rights reserved." FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource DarkTextColor}" Margin="0,15,0,8"/>

                            <TextBlock FontSize="13" Foreground="{StaticResource MediumTextColor}" Margin="0,5,0,5" HorizontalAlignment="Center" TextWrapping="Wrap">
                                <Run Text="For feedback or support, please contact: "/>
                                <Hyperlink NavigateUri="danielacostac03@gmail.com" RequestNavigate="Hyperlink_RequestNavigate">your.email@example.com</Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Border>
    </Viewbox>
</Window>